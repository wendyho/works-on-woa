---
const { isCurrent } = Astro.props;
---

<button
  class:list={[
    "py-2 px-6 rounded-2xl uppercase font-bold hover:scale-105",
    isCurrent ? "bg-blue-500 text-white" : "bg-white text-black",
  ]}
  class=""
>
  <slot />
</button>
