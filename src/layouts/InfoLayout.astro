---
import "../styles/infopage.css";
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout
  title={frontmatter.title + " | Windows on Arm Ready Software"}
  description={frontmatter.description}
  breadcrumbs={[{ href: `${Astro.url.pathname}`, title: frontmatter.title }]}
>
  <slot />
</BaseLayout>
